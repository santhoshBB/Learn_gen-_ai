user_query + session_id
  ↓
Classifier
  ├── Normal
  │     └── simple dict
  │
  ├── RAG
  │     └── Login -> simple dict->if login move it to Mem0 qdrant and clear the dict 
  │     └── Vector search (Qdrant)
  │     └── LLM (doc-grounded)
  │
  └── Workflow
        └── Login
        └── Mem0 (user_id)
        └── LangGraph
              ├── Plan        🔹 checkpoint
              ├── Tool call   🔹 checkpoint
              ├── Validate    🔹 checkpoint
              └── Respond
